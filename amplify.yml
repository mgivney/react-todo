version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - echo Building $AWS_BRANCH ...
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*